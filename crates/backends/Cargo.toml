[package]
name = "qb-backends"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
qb-core = { path = "../core" }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
